name: frontier_basic
description: Strategy for Frontier that uses basic authentication.
version: 1.0.0
repository: https://github.com/avesbox/frontier
topics: 
  - authentication
  - authorization
  - backend
  - basic
  - http

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  frontier_strategy: ^1.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
