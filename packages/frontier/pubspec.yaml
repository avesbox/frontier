name: frontier
description: Create your own authentication and authorization strategies for your web application.
version: 0.0.1
repository: https://github.com/serinus-nest/frontier
topics: 
  - authentication
  - authorization
  - backend
  - web
  - http

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  frontier_strategy: 0.0.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
